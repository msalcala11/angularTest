<h3>Let's Find You a Great One</h3>
<input typeahead="state for state in states | filter:$viewValue | limitTo:8" style="margin-bottom: 20px" type="text" class="form-control" placeholder="What kind of coach are you looking for?" ng-model="se">

<div class="sliderExample">
  <strong>Maximum distance from 30004</strong>
  <div ui-slider="{range: 'min'}" min="0" max="50" ng-model="distance"></div>
  <input type="text" ng-model="distance" size="3" /> miles
</div>

<div class="alert alert-info"> <strong style="font-size: medium">{{(coaches | filter:se | distanceFilter:distance).length}}</strong> coach<span ng-hide="(coaches | filter:se | distanceFilter:distance).length == 1">es</span> match<span ng-show="(coaches | filter:se | distanceFilter:distance).length == 1">es</span> your criteria.</div>
<div class="panel panel-default" ng-repeat="coach in coaches | filter:se | distanceFilter:distance">
  <div class="panel-body">
  <a href="">{{coach.name}}</a> ({{coach.distance}} miles away)
  <div>
    <span ng-repeat="specialty in coach.specialties"> {{specialty}}<span ng-show="! $last">,</span> </span>
  </div>
  </div>
</div>
